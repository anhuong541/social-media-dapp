{
  "name": "smart_contract",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "clean": "hardhat clean",
    "deploy:amoy": "hardhat run scripts/deploy-status.js --network polygonAmoy",
    "deploy:polygon": "hardhat run scripts/deploy-status.js --network polygon"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-network-helpers": "^1.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@types/node": "^22.0.0",
    "dotenv": "^16.4.7",
    "ethers": "^6.4.0",
    "hardhat": "^2.22.18",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
